<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd
           http://mybatis.org/schema/mybatis-spring
           http://mybatis.org/schema/mybatis-spring.xsd">

    <!-- 1. Component Scanning for Service Layer -->
    <!-- Scans the specified base package for Spring components like @Service, @Component, @Repository. -->
    <!-- @Controller components are typically excluded here and scanned by the MVC configuration. -->
    <context:component-scan base-package="com.example.hrms">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!-- 2. Load Database Properties -->
    <!-- Loads database connection details from 'db.properties' located in the classpath (src/main/resources). -->
    <!-- IMPORTANT: Users must configure 'db.properties' for their local environment. -->
    <context:property-placeholder location="classpath:db.properties"/>

    <!-- 3. Configure DataSource -->
    <!-- Defines the database connection pool. -->
    <!-- Uses properties loaded from 'db.properties'. -->
    <!-- DriverManagerDataSource is simple but not recommended for production. -->
    <!-- For production, consider using a more robust connection pool like HikariCP or Apache Commons DBCP2. -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${db.driverClassName}"/>
        <property name="url" value="${db.url}"/>
        <property name="username" value="${db.username}"/>
        <property name="password" value="${db.password}"/>
    </bean>

    <!-- 4. Configure MyBatis SqlSessionFactory -->
    <!-- Integrates MyBatis with Spring. -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <!-- Location of the global MyBatis configuration file. -->
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
        <!-- Locations of MyBatis XML mapper files. -->
        <!-- Scans for XML files in the specified package structure within the classpath. -->
        <property name="mapperLocations" value="classpath:com/example/hrms/mapper/xml/*.xml"/>
    </bean>

    <!-- 5. Configure MapperScannerConfigurer for MyBatis Mappers -->
    <!-- Scans for MyBatis mapper interfaces (e.g., UserMapper.java) in the specified base package. -->
    <!-- Automatically creates proxy beans for these mappers so they can be injected into services. -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.example.hrms.mapper"/>
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
    </bean>

    <!-- 6. Configure Spring TransactionManager -->
    <!-- Enables Spring's declarative transaction management. -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- 7. Enable Annotation-Driven Transaction Management -->
    <!-- Allows the use of @Transactional annotations on service methods. -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

</beans>
